{
  "name": "4front-dynamodb",
  "version": "0.0.2",
  "description": "DynamoDB metadata repository for 4front",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/_mocha --timeout 5000 --reporter spec --bail --check-leaks test/",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/ --timeout 5000",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks test/ --timeout 5000"
  },
  "author": "David Von Lehman <https://github.com/dvonlehman>",
  "repository": {
    "type": "git",
    "url": "https://github.com/4front/dynamodb"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "async": "^2.0.0-rc.6",
    "aws-sdk": "^2.4.0",
    "camel-case": "^3.0.0",
    "dateformat": "^1.0.12",
    "debug": "^2.2.0",
    "joi": "^8.4.2",
    "lodash": "^4.13.1",
    "moment": "^2.13.0",
    "simple-errors": "^1.0.1",
    "vogels": "^2.2.0"
  },
  "devDependencies": {
    "dash-assert": "^1.2.0",
    "eslint": "^2.12.0",
    "eslint-config-4front": "^1.2.4",
    "istanbul": "^0.4.3",
    "mocha": "^2.5.3",
    "shortid": "^2.2.6"
  }
}
